const toggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.main-nav');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');toggle.setAttribute('aria-expanded','false')}))}
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();
const form=document.getElementById('kontaktformular');const status=document.getElementById('formular-status');if(form&&status){form.addEventListener('submit',async e=>{e.preventDefault();const button=form.querySelector('button[type="submit"]');const original=button.textContent;button.disabled=true;button.textContent='Wird gesendet …';status.className='form-status';status.textContent='Ihre Nachricht wird übermittelt.';try{const response=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});if(!response.ok)throw new Error('Versand fehlgeschlagen');form.reset();status.className='form-status success';status.textContent='Vielen Dank. Ihre Anfrage wurde erfolgreich gesendet.';setTimeout(()=>{window.location.href='danke.html'},900)}catch(error){status.className='form-status error';status.textContent='Die Nachricht konnte nicht gesendet werden. Bitte versuchen Sie es erneut oder schreiben Sie an info@pvplanungshilfe.de.'}finally{button.disabled=false;button.textContent=original}})}
